# Skill: sensitive-info-in-vcs

## 触发条件
检测到用户提交到版本控制（GitHub）的文件中包含敏感信息（如用户名、密码、API密钥、路径中的个人标识）

## 行为规则
## 行为规则
1. **检测方法**
   - 检查提交的文件内容是否包含常见敏感模式：`<你的用户名>`、`password=`、`api_key=`、`token=` 等
   - 特别关注配置文件（`.json`、`.yaml`、`.env`、`config.*`）中的硬编码值
   - 结合文件变更历史，标记新增或修改的敏感内容

2. **修复建议**
   - 使用环境变量或密钥管理服务（如 AWS Secrets Manager）替代硬编码
   - 创建配置文件模板（如 `config.template.json`），将真实配置加入 `.gitignore`
   - 对于已提交的敏感信息，立即撤销提交并轮换密钥

3. **相关案例**
   - 案例1：用户在 `settings.json` 中硬编码了 Windows 用户名 `<你的用户名>`，导致个人路径暴露在 GitHub 上
   - 案例2：用户将数据库连接字符串（含密码）直接写在代码中并提交，造成安全风险

## 元数据
- 版本: 1
- 创建时间: 2026-05-21T20:32:39.328943
- 来源: 自动生成
