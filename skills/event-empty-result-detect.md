# Skill: event-empty-result-detect

## 触发条件
当系统执行 periodic_check、user_message 或 reflect 等关键动作后，日志中未记录任何有效结果、分析数据或产出数据为空时触发。若连续多次（如3次以上）结果均为空，则判定触发。

## 行为规则
### 1. 检测方法
- **日志完整性检查**: 扫描指定时间段内（例如：最近1小时）的日志，查找 `periodic_check`、`user_message`、`reflect` 等关键事件。检查这些事件后，是否存在对应的 `result`、`output`、`analysis` 等字段。
- **数据流追踪**: 如果日志不完整，追踪这些关键动作的处理函数，确认其是否正常执行并返回了数据。重点检查函数末端的 `return` 语句或日志记录语句 (`logger.info`, `print` 等)。
- **空值断言**: 在测试环境中，对上述动作的返回值进行断言，确保其不为 `None` 或空列表/字典。
- **日志收集流程检查**: 确认是否因日志级别过滤、采集中断、异步或缓存等原因导致结果未被记录或丢失。
- **连续空结果判定**: 若连续多次（如3次以上）结果均为空，则判定为异常。

### 2. 修复建议
- **强制日志记录**: 在所有关键动作（`periodic_check`, `user_message`, `reflect`）的处理函数末尾，强制添加日志记录语句，即使没有产出有效数据，也要记录一个明确的“无数据”状态（例如：`logger.info("periodic_check completed, result is empty")`）。
- **事件处理优化**: 审查 `user_message` 和 `reflect` 的事件处理流程。确保接收用户消息后，至少进行基础的情绪分析或关键词提取；确保反思机制至少输出一个“无新见解”的结论。
- **统一输出格式**: 为所有动作定义统一的输出结构（例如：`{"status": "success/failed/empty", "data": ...}`），以便后续模块或日志系统能明确知晓处理结果。
- **异常捕获与告警**: 对空结果情况添加默认处理或异常告警，如增加异常捕获并记录错误。
- **日志管道检查**: 检查日志收集管道是否正常，确认日志是否被正确写入、传输和存储，避免数据丢失或截断。

### 3. 相关案例
- **案例1**: 用户发送消息“你好”，系统日志中 `user_message` 事件后没有任何 `analysis` 记录，导致后续推荐模块无法工作。修复后，系统强制在收到消息后记录 `analysis: {"intent": "greeting", "entities": []}`。
- **案例2**: `periodic_check` 每小时执行一次，但连续多次日志显示该事件后无任何输出。修复后，在检查函数末尾添加 `logger.info(f"Periodic check finished. Status: {check_result_status}")`，即使检查结果为“无更新”，也进行了记录。
- **案例3**: `periodic_check` 返回空列表，经查是数据库查询条件错误，修复后恢复。
- **案例4**: `user_message` 处理后无输出，原因是消息解析失败未记录错误，增加异常捕获后解决。
- **案例5**: `user_message` 触发后，日志仅记录 'received' 但无后续分析结果，实际是处理函数未执行解析逻辑。修复后增加对消息内容的分类和状态更新。
- **案例6**: `periodic_check` 多次触发但结果字段为空，经查是检查函数未正确连接数据源。修复数据源连接后，日志正常输出检查结果。

## 元数据
- 版本: 5
- 创建时间: 2026-05-21T18:29:40.883099
- 来源: 去重合并
